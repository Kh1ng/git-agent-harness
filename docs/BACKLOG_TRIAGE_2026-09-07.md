# GAH backlog triage, 2026-09-07

GitHub currently lists **72 open issues**. This includes epics, research questions and overlapping scopes; it is not 72 independent implementation tasks. Read-only issue review; nothing closed or remotely updated.

The order below follows the current owner requirement: native Windows Tauri management, optional independent WSL worker, and iOS/Android control-only clients with QR pairing. The five completed/current sweep tickets (#1112, #1115, #1116, #1074, #946) are excluded.

## Next eight tickets

| Order | Ticket | Concrete next delivery | Dependencies / evidence |
| --- | --- | --- | --- |
| 1 | [#938 — central/worker role contract](https://github.com/Kh1ng/git-agent-harness/issues/938) | Reconcile the role contract with the actual worker HTTP service; expose central/worker identity and central URL in health. Keep central memory/skill storage off workers. | Its body says workers have “no server”; `scripts/install-wsl-worker.sh` and `apps/server/src/bin.ts` in `5a2be52` intentionally run a worker server for remote dispatch. Update this contradictory requirement before another implementation follows it. Reuse existing registry/execution identities. |
| 2 | [#741 — canonical environment/readiness](https://github.com/Kh1ng/git-agent-harness/issues/741) | Use one resolved backend identity for status, doctor and dispatch; distinguish Windows and WSL executables, authentication, health age and routing eligibility. | Depends on the clarified #938 contract. `docs/WINDOWS_NODE_SETUP.md` explicitly says an executable listing does not prove authentication/dispatch compatibility. A newly registered Windows worker currently has no project profiles. Preserve unknown facts; no all-CLIs-required gate. |
| 3 | [#917 — install command and installer CI](https://github.com/Kh1ng/git-agent-harness/issues/917) | Finish role/OS command coverage and run macOS installation in a scratch home on CI. Verify the Windows command installs the intended desktop plus independent worker. | `5a2be52` already supplies the Windows Settings command, installer and WSL setup; reuse them. Windows execution validation remains assigned to the user's Windows Claude/manual test, not a new competing agent. Follow #938/#741 for readiness and role assertions. |
| 4 | [#943 — reachable address and HTTPS onboarding](https://github.com/Kh1ng/git-agent-harness/issues/943) | Provide a reproducible second-device connection procedure using its configured central address, supported DNS and HTTPS; verify Windows and mobile connectivity. | Works alongside #917. Do not restore the issue's hardcoded `100.118.97.79`/`hermesagent` defaults: `docs/CONTROL_SURFACES.md` requires pairing/manual addresses, not a private address in app builds. Tailscale may be a supported option; the user also requires local-IP onboarding. |
| 5 | [#532 — authenticated mutations](https://github.com/Kh1ng/git-agent-harness/issues/532) | Close the HTTP/WebSocket authorization gap, enforce revocable per-device credentials, and keep mutation idempotency/audit. Implement the existing QR acceptance contract as a bounded child/change under this security work. | QR pairing is specified in `docs/CONTROL_SURFACES.md`, not implemented. Codes must expire, be single-use, and exchange for separate device credentials; revocation must cover HTTP and WS. Never encode the central administrator token. Depends on reachable transport (#943) and the existing operation contract; do not invent another command API. |
| 6 | [#949 — remote project import](https://github.com/Kh1ng/git-agent-harness/issues/949) | Import the repository/profile on the selected Windows/WSL worker and show its owning node in the project rail; migrate existing catalog entries without deleting checkouts. | #944 is closed; #946 supplies the fleet view. Requires #741 readiness and #532 authorization. This closes the current manual clone/profile/registration gap documented in Windows setup. The issue already identifies its catalog schema migration and offline failure rules. |
| 7 | [#968 — chat worker selection](https://github.com/Kh1ng/git-agent-harness/issues/968) | Show eligible nodes for the selected project; route the next turn to the selected node and attribute the result accurately. | Explicit issue dependencies: #944 (closed), #946 (current sweep). Reuse #946 cached observations/WS invalidation and #949 ownership; no new poller or independent health cache. Verify selection changes execution, not only the displayed label. |
| 8 | [#529 — shared reconnect/auth client](https://github.com/Kh1ng/git-agent-harness/issues/529) | Establish shared host-supplied credentials, compatibility checks and bounded event replay for desktop/browser/mobile; migrate existing client paths incrementally. | Explicit dependencies: #519 typed read API parity and #532. Audit remaining #519 operations before scheduling this as unblocked. This is the stated prerequisite for #534 mobile/PWA, which in turn precedes #526 native Android/iOS packaging. |

## Preserve assignments and mobile sequence

- **#1124 stays with Vibe** in its existing worktree/handoff.
- **#936 stays with AGY 2 / iOS**: Safari/device validation first, then the agreed shell. Do not create a second iOS implementation.
- **Windows build/install testing stays manual with Windows Claude/the owner** and the existing bundle/handoff. Native Windows agent execution remains distinct from working WSL execution; a successful build does not prove either runtime.
- After #529/#532, schedule **#534** (installable control-only PWA, stale offline state, no queued mutations) and **#526** (Android debug CI + iOS simulator packaging, secure storage, no local server/worker). Coordinate #526's iOS portion with #936. QR's shared exchange contract belongs before two separate native scanner implementations.

## Obsolete or overlapping candidates to reconcile, not auto-close

- **#938:** “worker has no server” conflicts with the current remote-dispatch service. The central-only database/skill-bank distinction remains valid. Correct the contract rather than removing required worker transport.
- **#917:** the claim that no UI-generated install command exists is partly superseded by `5a2be52`. Windows verification, other role/OS coverage and real macOS installer CI remain; this ticket is not complete.
- **#942:** “Windows follows proven macOS/iOS” is obsolete scheduling given the owner's current Windows-first request. Its Windows acceptance overlaps the current handoff/#917; Android packaging overlaps #526. Keep it as a platform umbrella, not another Windows implementation.
- **#760 and #741:** both require status/availability agreement. #760 has a narrow remaining criterion: Settings must stop using boot-time backend status while preserving GitHub/GitLab SCM status. Fold that regression into #741's delivery plan or retain it as a small child; do not build two availability registries.
- **#795 and #938:** both own node/worker contract decisions. Existing registry, claims and worker service provide concrete implementation evidence now. Resolve the remaining lifecycle/transport questions in one contract; avoid reopening a generic discovery project before shipping the worker.
- **#798:** the device-role decision is partly resolved by `docs/CONTROL_SURFACES.md`: phones control only; desktop can also host workers. Device-specific interaction verification remains useful; the role question no longer needs owner clarification.
- **#937 and #1074:** presence/startup/connection work overlaps the current desktop changes, but #937's local central role toggle and launchd service management remain real gaps. `scripts/install-macos.sh` still explicitly says no launchd equivalent is implemented.
- **#949:** curated project list and local Git import are explicitly marked shipped in its body (`f9610d7`). Implement only the remaining remote ownership/import scope.
- **#939:** treat as chat umbrella and track its worker slice through #968. Avoid a second node selector or freshness source.
- **#534 / #526 / #936:** distinguish responsive/PWA behavior, reproducible native packaging, and assigned iOS device acceptance. They overlap in UI reuse but are not interchangeable proof.

The map/Factory research and redesign issues (#790–#800, #1076, #1077) can follow a demonstrated install → ready worker → remote project → routed chat flow. Their titles do not make them release blockers for that flow.
