# Repository notes
- Dispatch ledger entries should record the resolved effective model after routing/backend selection.
- Tests should assert the serialized ledger JSON rather than inferring the model from mode.
