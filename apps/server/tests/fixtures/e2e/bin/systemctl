#!/bin/sh
if [ "$1" = "--user" ] && [ "$2" = "show" ]; then
  printf '%s\n' \
    'LoadState=loaded' \
    'ActiveState=active' \
    'MainPID=4242' \
    'ActiveEnterTimestamp=Sat 2026-08-22 12:00:00 UTC'
  exit 0
fi
exit 1
